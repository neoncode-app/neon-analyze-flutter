/// Neon Code shared lint config. The rules ship as YAML in
/// `analysis_options.yaml`; custom rules live in the `neon_lints` analyzer
/// plugin under `plugin/`. This file is only the package library entry point.
library;
